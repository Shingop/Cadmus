<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GitHub Profile</title>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;500;700&family=Syne:wght@400;700;800&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #080c0f;
      --grid: rgba(80, 200, 120, 0.07);
      --grid-accent: rgba(80, 200, 120, 0.15);
      --green: #50c878;
      --green-dim: rgba(80, 200, 120, 0.5);
      --text: #c8d8c8;
      --text-dim: #5a7a5a;
      --card-bg: rgba(10, 18, 12, 0.85);
      --border: rgba(80, 200, 120, 0.12);
    }

    html, body {
      min-height: 100%;
      background-color: var(--bg);
      color: var(--text);
      font-family: 'JetBrains Mono', monospace;
      overflow-x: hidden;
    }

    /* Grid background */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(var(--grid) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid) 1px, transparent 1px),
        linear-gradient(var(--grid-accent) 1px, transparent 1px),
        linear-gradient(90deg, var(--grid-accent) 1px, transparent 1px);
      background-size: 40px 40px, 40px 40px, 200px 200px, 200px 200px;
      background-position: -1px -1px, -1px -1px, -1px -1px, -1px -1px;
      pointer-events: none;
      z-index: 0;
    }

    /* Radial vignette */
    body::after {
      content: '';
      position: fixed;
      inset: 0;
      background: radial-gradient(ellipse 80% 60% at 50% 0%, transparent 40%, var(--bg) 100%);
      pointer-events: none;
      z-index: 0;
    }

    .page {
      position: relative;
      z-index: 1;
      max-width: 900px;
      margin: 0 auto;
      padding: 60px 24px 100px;
    }

    /* Header */
    header {
      display: flex;
      align-items: flex-start;
      gap: 36px;
      margin-bottom: 60px;
      animation: fadeUp 0.8s ease both;
    }

    .avatar-wrap {
      position: relative;
      flex-shrink: 0;
    }

    .avatar {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      background: linear-gradient(135deg, #1a2e1a, #0d1f10);
      border: 1.5px solid var(--green);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 36px;
      box-shadow: 0 0 30px rgba(80, 200, 120, 0.2), inset 0 0 20px rgba(80, 200, 120, 0.05);
    }

    .avatar-glow {
      position: absolute;
      inset: -8px;
      border-radius: 50%;
      border: 1px solid rgba(80, 200, 120, 0.2);
      animation: pulse 3s ease-in-out infinite;
    }

    .header-info { padding-top: 4px; }

    .name {
      font-family: 'Syne', sans-serif;
      font-size: 2rem;
      font-weight: 800;
      color: #fff;
      letter-spacing: -0.02em;
      line-height: 1;
      margin-bottom: 6px;
    }

    .handle {
      color: var(--green);
      font-size: 0.85rem;
      letter-spacing: 0.05em;
      margin-bottom: 12px;
    }

    .bio {
      font-size: 0.8rem;
      color: var(--text-dim);
      line-height: 1.7;
      max-width: 440px;
    }

    .stats-row {
      display: flex;
      gap: 24px;
      margin-top: 16px;
      flex-wrap: wrap;
    }

    .stat {
      font-size: 0.75rem;
      color: var(--text-dim);
    }

    .stat span {
      color: var(--text);
      font-weight: 500;
    }

    /* Section label */
    .section-label {
      font-size: 0.65rem;
      letter-spacing: 0.18em;
      color: var(--green);
      text-transform: uppercase;
      margin-bottom: 16px;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .section-label::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    /* Contribution grid */
    .contrib-section {
      margin-bottom: 50px;
      animation: fadeUp 0.8s 0.2s ease both;
    }

    .contrib-grid {
      display: grid;
      grid-template-columns: repeat(52, 1fr);
      gap: 3px;
    }

    .contrib-cell {
      aspect-ratio: 1;
      border-radius: 2px;
      background: rgba(80, 200, 120, 0.04);
      border: 1px solid rgba(80, 200, 120, 0.06);
      transition: background 0.2s;
      cursor: default;
    }

    .contrib-cell:hover { background: rgba(80, 200, 120, 0.3) !important; }
    .contrib-cell.l1 { background: rgba(80, 200, 120, 0.12); }
    .contrib-cell.l2 { background: rgba(80, 200, 120, 0.28); }
    .contrib-cell.l3 { background: rgba(80, 200, 120, 0.52); }
    .contrib-cell.l4 { background: rgba(80, 200, 120, 0.8); }

    /* Repo cards */
    .repos {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 14px;
      margin-bottom: 50px;
      animation: fadeUp 0.8s 0.35s ease both;
    }

    .repo-card {
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 6px;
      padding: 18px 20px;
      transition: border-color 0.2s, box-shadow 0.2s;
      position: relative;
      overflow: hidden;
    }

    .repo-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--green-dim), transparent);
      opacity: 0;
      transition: opacity 0.2s;
    }

    .repo-card:hover {
      border-color: rgba(80, 200, 120, 0.3);
      box-shadow: 0 0 20px rgba(80, 200, 120, 0.06);
    }

    .repo-card:hover::before { opacity: 1; }

    .repo-name {
      font-size: 0.85rem;
      color: var(--green);
      font-weight: 500;
      margin-bottom: 6px;
    }

    .repo-desc {
      font-size: 0.72rem;
      color: var(--text-dim);
      line-height: 1.6;
      margin-bottom: 14px;
    }

    .repo-meta {
      display: flex;
      gap: 16px;
      font-size: 0.68rem;
      color: var(--text-dim);
    }

    .repo-meta .dot {
      width: 8px; height: 8px;
      border-radius: 50%;
      display: inline-block;
      margin-right: 4px;
      vertical-align: middle;
    }

    /* Activity feed */
    .activity {
      animation: fadeUp 0.8s 0.5s ease both;
    }

    .activity-item {
      display: flex;
      gap: 14px;
      padding: 12px 0;
      border-bottom: 1px solid var(--border);
      font-size: 0.75rem;
    }

    .activity-item:last-child { border-bottom: none; }

    .activity-icon {
      width: 22px; height: 22px;
      border-radius: 50%;
      background: rgba(80, 200, 120, 0.08);
      border: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      font-size: 11px;
      margin-top: 1px;
    }

    .activity-text { color: var(--text-dim); line-height: 1.6; }
    .activity-text strong { color: var(--text); font-weight: 500; }
    .activity-text .repo-ref { color: var(--green); }
    .activity-time { margin-left: auto; color: #3a5a3a; font-size: 0.68rem; flex-shrink: 0; padding-top: 2px; }

    /* Animations */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(16px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.4; }
      50%       { transform: scale(1.08); opacity: 0.8; }
    }

    /* Cursor crosshair dot */
    .cursor-dot {
      position: fixed;
      width: 4px; height: 4px;
      border-radius: 50%;
      background: var(--green);
      pointer-events: none;
      z-index: 9999;
      transition: transform 0.1s;
      box-shadow: 0 0 8px var(--green);
    }
  </style>
</head>
<body>

<div class="cursor-dot" id="cursor"></div>

<div class="page">

  <header>
    <div class="avatar-wrap">
      <div class="avatar">⬡</div>
      <div class="avatar-glow"></div>
    </div>
    <div class="header-info">
      <div class="name">Alex Mercer</div>
      <div class="handle">// @alexmercer</div>
      <div class="bio">Systems engineer. Building tools at the intersection of distributed infra and developer experience. Open source contributor.</div>
      <div class="stats-row">
        <div class="stat"><span>142</span> repos</div>
        <div class="stat"><span>3.2k</span> followers</div>
        <div class="stat"><span>891</span> following</div>
        <div class="stat"><span>San Francisco, CA</span></div>
      </div>
    </div>
  </header>

  <div class="contrib-section">
    <div class="section-label">contributions — 2026</div>
    <div class="contrib-grid" id="contrib-grid"></div>
  </div>

  <div class="section-label">pinned repositories</div>
  <div class="repos">
    <div class="repo-card">
      <div class="repo-name">⬡ flux-runtime</div>
      <div class="repo-desc">Lightweight async task scheduler with backpressure support for distributed Go services.</div>
      <div class="repo-meta">
        <span><span class="dot" style="background:#00add8"></span>Go</span>
        <span>★ 1.4k</span>
        <span>⑂ 203</span>
      </div>
    </div>
    <div class="repo-card">
      <div class="repo-name">⬡ kube-scaffold</div>
      <div class="repo-desc">CLI to bootstrap production-ready Kubernetes manifests with sensible defaults and policy enforcement.</div>
      <div class="repo-meta">
        <span><span class="dot" style="background:#50c878"></span>Shell</span>
        <span>★ 876</span>
        <span>⑂ 91</span>
      </div>
    </div>
    <div class="repo-card">
      <div class="repo-name">⬡ obsidian-cli</div>
      <div class="repo-desc">Terminal-first note management layer that syncs with your Obsidian vault over WebDAV.</div>
      <div class="repo-meta">
        <span><span class="dot" style="background:#e06c00"></span>Rust</span>
        <span>★ 512</span>
        <span>⑂ 44</span>
      </div>
    </div>
    <div class="repo-card">
      <div class="repo-name">⬡ delta-sync</div>
      <div class="repo-desc">Efficient binary delta synchronisation library with pluggable transport backends.</div>
      <div class="repo-meta">
        <span><span class="dot" style="background:#e06c00"></span>Rust</span>
        <span>★ 338</span>
        <span>⑂ 29</span>
      </div>
    </div>
  </div>

  <div class="activity">
    <div class="section-label">recent activity</div>
    <div class="activity-item">
      <div class="activity-icon">↑</div>
      <div class="activity-text">Pushed 3 commits to <span class="repo-ref">flux-runtime</span> — <strong>fix: resolve goroutine leak in task pool cleanup</strong></div>
      <div class="activity-time">2h ago</div>
    </div>
    <div class="activity-item">
      <div class="activity-icon">★</div>
      <div class="activity-text">Starred <span class="repo-ref">tigerbeetle/tigerbeetle</span></div>
      <div class="activity-time">5h ago</div>
    </div>
    <div class="activity-item">
      <div class="activity-icon">⑂</div>
      <div class="activity-text">Opened PR <strong>#88</strong> in <span class="repo-ref">kube-scaffold</span> — add support for KEDA-based HPA autoscaling</div>
      <div class="activity-time">1d ago</div>
    </div>
    <div class="activity-item">
      <div class="activity-icon">◎</div>
      <div class="activity-text">Commented on issue <strong>#412</strong> in <span class="repo-ref">delta-sync</span></div>
      <div class="activity-time">2d ago</div>
    </div>
    <div class="activity-item">
      <div class="activity-icon">↑</div>
      <div class="activity-text">Pushed 7 commits to <span class="repo-ref">obsidian-cli</span> — <strong>feat: add WebDAV conflict resolution strategy</strong></div>
      <div class="activity-time">3d ago</div>
    </div>
  </div>

</div>

<script>
  // Generate contribution grid
  const grid = document.getElementById('contrib-grid');
  const levels = [null, 'l1', 'l2', 'l3', 'l4'];
  const weights = [0.45, 0.25, 0.15, 0.1, 0.05];

  function weightedRandom() {
    const r = Math.random();
    let cumulative = 0;
    for (let i = 0; i < weights.length; i++) {
      cumulative += weights[i];
      if (r < cumulative) return levels[i];
    }
    return null;
  }

  for (let i = 0; i < 52 * 7; i++) {
    const cell = document.createElement('div');
    cell.className = 'contrib-cell';
    const lvl = weightedRandom();
    if (lvl) cell.classList.add(lvl);
    grid.appendChild(cell);
  }

  // Cursor dot
  const cursor = document.getElementById('cursor');
  document.addEventListener('mousemove', e => {
    cursor.style.left = (e.clientX - 2) + 'px';
    cursor.style.top  = (e.clientY - 2) + 'px';
  });
</script>
</body>
</html>
